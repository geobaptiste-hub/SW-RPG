## 1. Plus jamais deux soins voisins
- `map_service.dart` — `populateNewlyDiscoveredTiles` : dans le `case 'soin'`, vérifier les 8 cases voisines (Chebyshev 1) — à la fois dans `after.tiles` (soins déjà posés avant) et dans la liste `tiles` du passage en cours (soins posés dans la même révélation). Si un voisin a déjà un `healSite` → la case reste vide (pas de nouvel événement tiré).
- `game_service.dart` — `_clearTileEvent` : quand on redépose un `healSite`, exclure des candidates les cases voisines d'un soin existant.

## 2. Faction du boss sur l'écran Joueurs
- `players_screen.dart` : la chip « 🏆 AT-AT » devient « 🏆 AT-AT (Empire) » via `PlanetConstants.bossFactionByType[boss]!.displayName` (import planet_constants à ajouter).

## 3. Game Over Big Four : la faction gagnante
- `game_over_screen.dart` — `resolveWinner` : en mode Big Four uniquement, les deux titres d'équipe (« L'équipe D remporte la partie ! ») deviennent « La faction Empire remporte la partie ! » (majorité de faction parmi les membres de l'équipe gagnante — Big Four = équipes mono-faction). Le mode Équipes classique garde « L'équipe A… ». Helper interne `winnerFaction(members)` (majorité). Phrasing uniforme : « La faction X remporte la partie ! » (pas de souci d'élision).
- Test : état Big Four où l'équipe Empire atteint 3 boss adverses → `resolveWinner` renvoie le titre faction.

## 4. Splash sans texte
- `splash_screen.dart` : suppression du bloc « STAR WARS / R P G » — il ne reste que l'image en plein écran (ton `splash.jpg` est déjà déployé).

## Validation
Tests (invariant « pas deux soins voisins » sur un populate complet, titre faction Big Four) + `flutter analyze` / `flutter test` / `flutter build web` / `flutter build windows` + note de passation.